# ===============================
# BUG 1 - RUNTIME ERROR
# ===============================

# Type   : RuntimeError (IndexError)
# Cause  : Trying to access an index that does not exist
# Fix    : Removed invalid index access and used correct list methods

# BUGGY CODE:
"""
marks = []

for i in range(5):
    mark = int(input("Enter marks: "))
    marks.append(mark)

print("Total Marks:", sum(marks))
print("Average Marks:", sum(marks) / len(marks))
print("Highest Marks:", marks[10])  # Runtime Error (IndexError)
print("Lowest Marks:", min(marks))
"""

# FIXED CODE:
marks = []

for i in range(5):
    mark = int(input(f"Enter marks of student {i+1}: "))
    marks.append(mark)

print("\n----- RESULT -----")
print("Total Marks:", sum(marks))
print("Average Marks:", sum(marks) / len(marks))
print("Highest Marks:", max(marks))   # Fixed
print("Lowest Marks:", min(marks))


# ===============================
# BUG 2 - LOGICAL ERROR
# ===============================

# Type   : Logical Error
# Cause  : Wrong formula used for average calculation
# Fix    : Divided by total number of students (5)

# BUGGY CODE:
"""
average = sum(marks) / 2   # Wrong logic
print("Average Marks:", average)
"""

# FIXED CODE:
average = sum(marks) / len(marks)
print("Correct Average Marks:", average)




import json
import os

FILE_NAME = "expenses.json"


# Load expenses from JSON file
def load_expenses():
    if os.path.exists(FILE_NAME):
        with open(FILE_NAME, "r") as file:
            return json.load(file)
    return []


# Save expenses to JSON file
def save_expenses(expenses):
    with open(FILE_NAME, "w") as file:
        json.dump(expenses, file, indent=4)


# Add expense
def add_expense(expenses):
    name = input("Enter expense name: ")
    amount = float(input("Enter amount: "))

    expense = {
        "name": name,
        "amount": amount
    }

    expenses.append(expense)
    save_expenses(expenses)

    print("Expense added successfully!")


# View expenses
def view_expenses(expenses):
    if not expenses:
        print("No expenses found.")
        return

    print("\nExpenses List:")
    for i, expense in enumerate(expenses, start=1):
        print(f"{i}. {expense['name']} - ₹{expense['amount']}")


# Delete expense
def delete_expense(expenses):
    view_expenses(expenses)

    try:
        index = int(input("Enter expense number to delete: ")) - 1

        if 0 <= index < len(expenses):
            deleted = expenses.pop(index)
            save_expenses(expenses)
            print(f"{deleted['name']} deleted successfully!")
        else:
            print("Invalid expense number.")

    except ValueError:
        print("Please enter a valid number.")


# Total spending
def total_spending(expenses):
    total = sum(expense["amount"] for expense in expenses)
    print(f"Total Spending: ₹{total}")


# Main Menu
def main():
    expenses = load_expenses()

    while True:
        print("\n===== Expense Tracker CLI =====")
        print("1. Add Expense")
        print("2. View Expenses")
        print("3. Delete Expense")
        print("4. Total Spending")
        print("5. Exit")

        choice = input("Enter your choice: ")

        if choice == "1":
            add_expense(expenses)

        elif choice == "2":
            view_expenses(expenses)

        elif choice == "3":
            delete_expense(expenses)

        elif choice == "4":
            total_spending(expenses)

        elif choice == "5":
            print("Exiting program...")
            break

        else:
            print("Invalid choice. Try again.")


main()
